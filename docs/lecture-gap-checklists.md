# Чек-листы доработки проекта по материалам лекций

Дата аудита: 25.09.2026. Сравнивались конспекты и 24 слайда разделов «Анализ», «Разработка», «Тестирование», структура примера `summer-school-2026-feature-client` и текущее состояние проекта.

Обозначения: **P0** — исправить до сдачи; **P1** — важно для соответствия лекциям; **P2** — полезное усиление. `[x]` означает, что артефакт уже существует и был проверен, `[ ]` — необходимая работа.

## Краткий вывод

Формальные условия задания выполнены: есть MVP, архитектура, восемь пользовательских возможностей, 25 тест-кейсов, 24 автотеста, три исправленных бага, task/bug-документы, промпты и Git-история. Требования, дизайн-брифы, мобильное ТЗ, матрица трассировки и QA-ревью уже формализованы, но ещё нужны раздельные layout/component/scenario-наборы. В коде есть риски повторной брони при сетевом retry и ошибочных UI-состояний.

## Анализ

### Что уже сделано

- [x] Сохранены сырой бриф и уточнения: `docs/source/brief-cooking.md`, `docs/00-customer-brief.md`.
- [x] Описаны вопросы и допущения, 6 BR, 16 FR, 12 NFR, 6 User Stories и 4 Use Cases.
- [x] Зафиксирована исходная граница MVP с внешним backend как black box.
- [x] Созданы архитектурная схема, ER-модель, OpenAPI и sequence diagrams.
- [x] Созданы реестр интерфейсных артефактов, foundations и отдельные дизайн-брифы для экранов и overlays MVP.
- [x] Созданы отдельные мобильные ТЗ для всех экранов/слоёв и семь переиспользуемых `LOGIC-*`.

### Что переделать или добавить

- [x] **P0 — устранить неподтверждённые заявления.** `docs/lecture-alignment.md` заменён фактической сверкой по изученным конспектам, 24 слайдам и примеру проекта; статусы анализа, разработки и тестирования разделены, устаревшее ограничение про CAPTCHA удалено.
- [x] **P0 — оформить изменение скоупа отдельным решением.** Созданы `ADR-001-own-backend.md` и журнал решений: исходный бриф сохранён, а собственный FastAPI/backend оформлен как учебное расширение с причинами, границами и последствиями.
- [x] **P1 — разделить выявление требований.** Созданы `customer-questions.md` с вопросами, ответами, статусами и источниками и `domain-description.md` с ролями, сущностями, событиями, бизнес-правилами и границами домена; `elicitation.md` служит индексом.
- [x] **P1 — разнести требования по каноническим файлам:** созданы `business-requirements.md`, `functional-requirements.md`, `non-functional-requirements.md`; у каждой записи есть стабильный ID, MoSCoW-приоритет, источник и проверяемый критерий.
- [x] **P1 — исправить идентификаторы.** Новые пространства `BR-*`, `FR-*`, `NFR-*` непрерывны; исторические пропуски `R-*` объяснены, а legacy-ID сопоставлены каноническим требованиям без поломки TASK/BUG и тест-кейсов.
- [x] **P1 — сделать НФТ проверяемыми.** Указаны профиль измерения 2 секунд, viewport, способ проверки WCAG AA, конкурентный сценарий, правила сохранения данных, ограничения логирования и целевые платформы.
- [x] **P1 — усилить User Stories.** Все 6 историй оформлены как Card/Conversation/Confirmation, получили приоритет, источник, связи с BR/FR/NFR, Given/When/Then-критерии и INVEST-проверку.
- [x] **P1 — дополнить Use Cases.** Все 4 сценария содержат акторов, триггер, предусловия, постусловия, минимальную гарантию, основной поток и исключения, включая offline/timeout/retry, прокат и отказ push-разрешения.
- [x] **P0 — создать двунаправленную матрицу трассировки.** Создан `docs/01-analysis/traceability-matrix.md`: он связывает `BR → US/UC → FR/NFR → SCR/BS/LOGIC → API → код → TC → commit`, отдельно показывает неполное покрытие, а `test-cases.md` теперь содержит канонические и legacy-ID.
- [x] **P1 — подготовить реестр экранов и дизайн-брифы.** Введены ID для 3 экранов, sheet, modal, dialog и 2 компонентов; созданы foundations и отдельные постановки дизайнеру. Текущий Expo UI закреплён как временный baseline D-008, Figma не заявлена.
- [x] **P1 — создать мобильное ТЗ.** Для каждого `SCR/BS/MDL/DLG` описаны вход, lifecycle, состояния, элементы, действия, навигация, валидации, API/ошибки и acceptance criteria; общие правила вынесены в `LOGIC-001`–`LOGIC-007`.
- [x] **P1 — дополнить модель данных.** Зафиксированы read-only/mutable-владение, поля, nullability, enum, `IdempotencyRecord`, PostgreSQL constraints и уровень обеспечения каждого инварианта.
- [ ] **P0 — реализовать согласованный контракт бронирования в коде.** Требования, модель, OpenAPI и sequence diagram уже используют `201/409/410`, включая `410 SLOT_CANCELLED`. На этапе разработки нужно синхронизировать `ApiErrorCode`, HTTP adapter, стабильный ключ retry, mock/backend и contract-тесты.
- [x] **P1 — довести OpenAPI.** Добавлены 7 `operationId`, Bearer security, общий problem envelope, применимые `400/401/404/409/410/422/429/500/503`, примеры и точная семантика `Idempotency-Key`.
- [x] **P1 — провести отдельное QA-ревью требований.** Создан `docs/01-analysis/requirements-qa-review.md`: замечания классифицированы, проведены impact/platform review, исправлены auth-трассировка и покрытие метрики пути, D-009/D-010 внесены в журнал, открытые implementation/testing gaps маршрутизированы.
- [ ] **P1 — актуализировать реестр промптов.** Хранить точный текст каждого реально отправленного запроса в хронологическом порядке и ссылаться на него из соответствующей TASK/BUG-карточки.

## Разработка

### Что уже сделано

- [x] Реализованы domain policies, mock и HTTP-адаптер, push-модуль и восемь пользовательских возможностей.
- [x] Деньги хранятся в копейках, даты передаются как ISO 8601, предметные ошибки представлены кодами.
- [x] Все ссылки на указанные в задачах commits существуют; история логично разделена на docs/feat/test/fix.
- [x] `npm run typecheck`, `npm test` и `npm run export:web` проходят на текущем checkout.

### Что переделать или добавить

- [ ] **P0 — сделать retry бронирования идемпотентным.** `HttpStudioApi.createBooking()` создаёт новый ключ при каждом вызове (`src/data/httpStudioApi.ts`), поэтому потерянный ответ и повтор из формы могут создать две брони. Генерировать ключ на одну попытку пользователя и повторно использовать его для того же payload до однозначного результата.
- [ ] **P0 — обновлять данные после конфликта.** `handleBook` показывает текст ошибки, но вопреки UC-01 и архитектуре не обновляет расписание после `SLOT_FULL`, `SLOT_CANCELLED` или `RENTAL_UNAVAILABLE`. Сохранить заполненную форму, обновить слот и показать актуальное состояние.
- [ ] **P0 — разделить успех мутации и сбой refresh.** Сейчас успешная бронь/отмена с последующей ошибкой `refresh()` попадает в общий `catch` и выглядит как неуспешная операция. Это провоцирует повтор. Сначала зафиксировать результат мутации, затем отдельно обрабатывать устаревание данных.
- [ ] **P0 — реализовать настоящий Error State.** После ошибки начальной загрузки остаётся toast, а пустой массив превращается в «Пока нет доступных классов». Ввести состояния `initial/loading/content/empty/error/refreshing`, постоянное сообщение и кнопку «Повторить».
- [ ] **P0 — проверить push payload по контракту.** `isStudioCancellationNotification` проверяет только `type`, хотя OpenAPI требует `bookingId` и `reason`; текущий тест ошибочно признаёт payload без причины валидным. Валидировать все обязательные поля и использовать `bookingId` для адресного обновления/навигации.
- [ ] **P1 — декомпозировать `App.tsx` (1290 строк).** Вынести screens, modals, reusable UI, theme и application orchestration в feature-модули (`schedule`, `booking`, `bookings`, `review`, `profile`, `notifications`). Оставить в `App.tsx` composition root и навигацию. Обновить архитектурный документ под фактические модули.
- [ ] **P0 — перестроить репозиторий в monorepo.** Перенести текущее Expo-приложение в `client/`, создать `backend/`, оставить общие `docs/` и корневой `compose.yaml`. После переноса обновить команды, относительные ссылки, CI и инструкции, не смешивая зависимости Node.js и Python.
- [ ] **P1 — добавить два плана реализации.** Создать отдельные итеративные checklist-файлы для client и backend со scope, доменными сущностями, инструментами, этапами и критериями готовности.
- [ ] **P0 — зафиксировать backend-стек.** Python 3.12+, FastAPI, Pydantic, SQLAlchemy 2, Alembic, PostgreSQL, Uvicorn, pytest и httpx. Версии закрепить в `backend/pyproject.toml`; инструменты форматирования и анализа выбрать до начала реализации.
- [ ] **P1 — создать слоистую структуру backend.** Использовать `backend/app/api`, `domain`, `services`, `repositories`, `schemas`, `db` и composition root `main.py`. HTTP handlers не должны содержать транзакционную бизнес-логику.
- [ ] **P0 — определить источник истины API.** Сохранить контрактный подход: согласованный OpenAPI задаёт поведение, а схема FastAPI регулярно сравнивается с ним. Не допускать двух расходящихся спецификаций.
- [ ] **P0 — спроектировать PostgreSQL и миграции.** Добавить таблицы классов, шефов, программ, броней, отзывов, push-токенов и ключей идемпотентности; ограничения, внешние ключи, индексы и enum/check constraints должны обеспечивать инварианты модели. Проверить Alembic upgrade на пустой БД и downgrade хотя бы последней миграции.
- [ ] **P1 — добавить воспроизводимые seed-данные.** Создать шефов, программы, классы разных статусов, остатки мест/инвентаря и брони для всех demo-сценариев. Seed должен быть идемпотентным и запускаться отдельной командой, а не частью production startup.
- [ ] **P0 — реализовать endpoints по контракту.** Расписание, карточка класса, список/создание/отмена броней, отзыв и регистрация push-токена должны использовать единый error envelope и не возвращать неописанные поля/статусы.
- [ ] **P0 — обеспечить атомарное бронирование.** В одной транзакции блокировать слот, проверять статус, место, прокат и повторную активную бронь; не допускать отрицательных остатков. Хранить результат по `Idempotency-Key` и возвращать его при безопасном повторе того же payload.
- [ ] **P0 — обеспечить атомарную отмену.** Блокировать бронь и слот, применять границу 12 часов ровно по требованиям, возвращать место и прокат только один раз и корректно обрабатывать параллельные/повторные запросы.
- [ ] **P0 — принять решение по идентификации клиента.** Для учебного MVP выбрать явный механизм: минимальная Bearer-сессия тестового пользователя либо документированный dev-auth. Нельзя оставлять «текущего клиента» неявным в API и одновременно хранить пользовательские данные.
- [ ] **P1 — добавить конфигурацию и безопасность backend.** Использовать env/settings без секретов в Git, валидировать DSN/CORS/log level, не логировать аллергии и токены, добавить request ID, структурированные ошибки и health endpoint.
- [ ] **P0 — добавить Docker.** Создать `backend/Dockerfile` и корневой `compose.yaml` с PostgreSQL, backend, healthchecks и отдельным шагом миграций/seed. Локальный стек должен подниматься документированной командой без ручного создания таблиц.
- [ ] **P1 — унифицировать валидацию на границах.** Проверять лимиты аллергий (300) и комментария (500), допустимые enum и rating не только в UI, но и в mock/data-слое. Добавить безопасную проверку входящих DTO вместо безусловных TypeScript casts.
- [ ] **P1 — определить часовую зону студии.** `getScheduleQuery`, `filterClasses` и форматирование используют локальную зону устройства/строковый префикс. Зафиксировать IANA timezone и покрыть полуночные границы и пользователя в другой зоне.
- [ ] **P1 — сделать частичную загрузку устойчивой.** Ошибка одного `getClass()` для архивной брони сейчас срывает весь `refresh`. Отобразить доступные данные и отдельное состояние недоступной карточки.
- [ ] **P1 — исправить доступность.** Требование задаёт минимум 44×44, но сегменты имеют высоту 38, close-кнопка 42, а часть ghost/text controls не гарантирует 44. Добавить роли/selected state для нижней навигации и доступные названия кнопкам отмены/закрытия.
- [ ] **P1 — убрать ложные affordance.** «Аллергии и предпочтения» и «Помощь» выглядят как нажимаемые строки с chevron, но реализованы обычными `View`. Реализовать действия либо изменить визуальное представление.
- [ ] **P1 — настроить lint/format.** Добавить ESLint/Prettier (или выбранные аналоги), команды `npm run lint`/`format:check` и зафиксировать правила. Сейчас проверяется только TypeScript.
- [ ] **P1 — оформить каждую новую доработку по заданию.** Для каждого пункта создать TASK/BUG `.md` с целью/симптомом, требованиями, точным промптом, проверкой и commit.
- [ ] **P0 — привести Git к чистому состоянию перед сдачей.** `AGENTS.md` сейчас не отслеживается (`?? AGENTS.md`); добавить его осознанным commit вместе с актуальными правилами проекта. `node_modules`, `.npm-cache` и `dist` остаются ignored.
- [ ] **P1 — проверить зависимости.** `npm ci` сообщил о 10 moderate vulnerabilities и deprecated транзитивном `uuid@7.0.3`. Выполнить `npm audit`, определить production impact и обновить зависимости без автоматического `--force`, если это не ломает Expo.
- [ ] **P2 — добавить CI.** На push/PR выполнять clean install, typecheck, lint, tests и web-export; это превратит локальные заявления README в воспроизводимую проверку.

## Тестирование

### Что уже сделано

- [x] Есть 24 трассированных Markdown-кейса, 24 проходящих Vitest-теста и восемь screenshots ручной проверки.
- [x] Покрыты доменные границы отмены, цена, фильтры, mock-инварианты, HTTP-запросы и push subscriptions.
- [x] Три дефекта имеют отдельные документы и исправляющие commits.
- [x] Нативная APNs/FCM-доставка честно отмечена как непроверенная.

### Что переделать или добавить

- [ ] **P0 — исправить маркировку покрытия.** Кейсы с типом `UI/auto` часто автоматизируют только policy/mock, но не UI (например, сохранение выбранной даты, скрытие кнопки оценки, отображение причины). Разделить уровни `unit`, `contract`, `component UI`, `scenario/E2E`, `manual` и не отмечать непроверенный слой.
- [ ] **P1 — сначала сделать план покрытия.** Подготовить компактную таблицу требований и типов проверок, проверить ширину, затем детализировать выбранные кейсы — «сначала ширина, потом глубина».
- [ ] **P1 — разделить три набора:** layout-проверки, component/functional-проверки и законченные UC-сценарии. Не смешивать внешний вид, единичное правило и пользовательский flow в одном кейсе.
- [ ] **P1 — ввести `json_schema.yml` и валидные JSON-массивы.** Для каждого TestCase хранить `labels`, конкретный `summary`, `priority`, `estimate`, `testRepositoryPath`, `description/source` и шаги `action/data/result`. Markdown оставить как читаемый индекс/план.
- [ ] **P1 — создать layout-набор.** Проверить 360 px, длинные тексты, font scaling, клавиатуру, модальные окна, scroll, safe areas, контраст, touch targets и состояния Loading/Empty/Error. Привязать каждый кейс к design baseline/screenshot или Figma frame.
- [ ] **P0 — добавить UI component-тесты.** Проверить фильтры без сброса даты, форму и лимиты, disabled rental, error/retry, успешную навигацию, историю отмены, одноразовый review и доступные роли. Текущие Vitest-тесты не рендерят `App.tsx`.
- [ ] **P0 — покрыть сетевой retry.** Воспроизвести «backend создал бронь, ответ потерян» и доказать повтор с тем же idempotency key; проверить, что UI не предлагает создать вторую бронь после успешной мутации и неуспешного refresh.
- [ ] **P0 — покрыть Error против Empty.** Добавить проверки initial failure, retry, refresh поверх старого контента, частичный `getClass` failure, timeout/offline и восстановление сети.
- [ ] **P1 — расширить contract-тесты всех endpoints.** Проверить методы, headers, body, `200/201/204`, `400/401/404/409/410/422/429/5xx`, пустой/невалидный JSON, nullable/missing fields и отображение каждого error code в UI.
- [ ] **P0 — добавить backend unit/API-тесты.** Через pytest покрыть доменные правила и services; через httpx/ASGI проверить каждый FastAPI endpoint, dependency overrides, валидацию Pydantic и единый формат ошибок.
- [ ] **P0 — добавить интеграционные тесты PostgreSQL.** На отдельной тестовой БД применять реальные Alembic migrations и проверять repositories, constraints, rollback и seed. Не подменять такие проверки SQLite, поскольку блокировки и конкурентность отличаются.
- [ ] **P0 — добавить конкурентные тесты.** Параллельные create/cancel не должны создавать двойную бронь, уходить в отрицательные остатки или дважды возвращать место/прокат. Отдельно проверить одинаковый и конфликтующий `Idempotency-Key`.
- [ ] **P1 — добавить контрактную сверку.** Проверять, что OpenAPI, публикуемый FastAPI, совместим с каноническим контрактом и TypeScript-моделями клиента; несовместимое изменение должно падать в CI.
- [ ] **P0 — проверить полный интеграционный поток.** Запустить Docker-стек и пройти `Expo client → FastAPI → PostgreSQL` для расписания, бронирования, конфликта, отмены и отзыва. Mock оставить только как явно обозначенный demo/test fallback.
- [ ] **P1 — добавить граничные данные.** Аллергии 0/300/301, комментарий 0/500/501, rating 0/1/5/6/дробь, последний слот, последний прокатный набор, повторная отмена, неизвестные booking/class IDs, timezone и переход даты.
- [ ] **P1 — усилить push-тесты.** Проверить обязательные `bookingId/reason`, malformed payload, denied permission, ошибку регистрации токена, web/Android/iOS, foreground/open/cold-start, повтор события и отсутствие обновления для чужого типа.
- [ ] **P1 — создать сценарные UC-тесты.** Один тест — один полный flow: запись, конфликт, отмена до/после дедлайна, отзыв, отмена студией через push. Не дублировать ими unit/component проверки.
- [ ] **P1 — провести ревью тестов и матрицу покрытия.** Статусы для каждого требования: `полностью`, `частично`, `не покрыто`, `слабое покрытие`, `противоречие`; завести реестр конкретных FORMAT/COVERAGE/DECOMP/REQUEST/LAYOUT проблем. По правилам `AGENTS.md` субагента использовать только по прямой просьбе пользователя; иначе выполнить отдельный повторный проход самостоятельно.
- [ ] **P1 — усилить ручные доказательства.** Сейчас восемь screenshots не подтверждают все 16 строк со статусом «Пройден». Для каждого ручного кейса указать сборку, платформу, шаги, фактический результат и ссылку на screenshot/video/log либо честно отметить отсутствие доказательства.
- [ ] **P1 — дополнить карточки багов.** В `BUG-003` добавить шаги воспроизведения; для всех трёх сохранить failing-test/до и passing-test/после, затронутые требования и регрессионный набор.
- [ ] **P1 — настроить coverage threshold.** Снимать покрытие отдельно для domain/data/notifications/UI и установить реалистичный минимум после добавления component tests; процент не должен заменять матрицу требований.

## Рекомендуемый порядок выполнения

1. Оформить изменение скоупа, решить `409/410`, идентификацию клиента и источник истины OpenAPI.
2. Перестроить репозиторий в `client/`, `backend/`, `docs/`; сразу обновить команды и ссылки.
3. Поднять FastAPI + PostgreSQL через Docker, применить Alembic migrations и seed.
4. Реализовать и протестировать backend endpoints, транзакции, конкурентность и идемпотентность.
5. Исправить P0 клиента: устойчивый retry, refresh после конфликтов, error state, push payload и разделение mutation/refresh.
6. Декомпозировать `App.tsx`, затем добавить UI- и сквозные интеграционные тесты.
7. Собрать двунаправленную матрицу трассировки и QA-ревью, затем привести код и тестовый пайплайн к согласованному ТЗ.
8. Обновить TASK/BUG/prompts, ручные доказательства, README и итоговый отчёт; выполнить один финальный прогон и отдельные commits.

## Выполненная проверка текущего состояния

- `npm ci --cache .npm-cache` — успешно, 526 packages; npm сообщил о 10 moderate vulnerabilities.
- `npm run typecheck` — успешно.
- `npm test` — успешно: 5 файлов, 24/24 теста.
- `EXPO_NO_TELEMETRY=1 npm run export:web` — успешно, bundle создан в `dist/`.
- Локальные Markdown-ссылки — разрешаются.
- Все commit-хеши из TASK/BUG-документов — существуют.
- Рабочее дерево не чистое только из-за неотслеживаемого `AGENTS.md`; генерируемые каталоги ignored.
