# TASK-016 — Формализация BR, FR и NFR

## Цель

Разделить требования по уровням, сделать их проверяемыми и сохранить совместимость существующих задач и тестов со старыми `R-*`.

## Источники

- неизменяемый `docs/source/brief-cooking.md`;
- `docs/01-analysis/customer-questions.md` и `domain-description.md`;
- журнал решений и `ADR-001`;
- прежние MVP-требования, User Stories, Use Cases и тест-кейсы.

## Промпт

`docs/prompts/016-formalize-requirements.md`.

## Выполнено

- созданы непрерывные пространства `BR-*`, `FR-*` и `NFR-*`;
- каждому требованию назначены приоритет, источник и проверяемый критерий;
- НФТ получили конкретные условия измерения;
- старые `R-*` сохранены в таблице соответствия без массового изменения ссылок;
- зафиксированы причина пропусков legacy-ID и приоритет канонических документов;
- в Use Case исправлен устаревший `409 SLOT_CANCELLED` на принятый `410`.

## Проверка

- [x] Все ID внутри каждого нового пространства уникальны и непрерывны.
- [x] Каждое требование содержит MoSCoW-приоритет и источник.
- [x] Каждое legacy-требование сопоставлено хотя бы одному каноническому ID.
- [x] Нерешённые production-вопросы не представлены как обязательства MVP.
- [x] Исходный бриф не изменён.
- [x] Markdown не содержит битых локальных ссылок или trailing whitespace.

## Commit

`a6dd292` — `docs: formalize business functional and quality requirements`.
