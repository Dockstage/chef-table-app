# TASK-007 — Тестирование MVP

## Цель

Подтвердить бизнес-правила и найти воспроизводимые дефекты до ручной приёмки.

## Требования

R-001–R-012.

## Промпт

`docs/prompts/004-testing.md`.

## Выполнено в исходном тестовом commit

- 16 ручных/API тест-кейсов;
- 11 автоматических тестов domain и Mock API;
- граничные проверки дедлайна отмены;
- негативные проверки повторной записи и отменённого класса.

## Проверка

- `npm run typecheck`;
- `npm test`;
- `npm run export:web`;
- ручной проход ключевых экранов в web-preview.

После устранения расхождений набор расширен до 22 тест-кейсов и 24 автоматических тестов, включая HTTP-адаптер, полный push lifecycle, расширенный период, прокатный фонд и историю отмен.

## Commit

`e67cfe3` — `test: add MVP cases and expose booking defects`.
